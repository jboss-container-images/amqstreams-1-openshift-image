schema_version: 1

name: bridge
version: 1.6.6

envs:
  - name: "STRIMZI_HOME"
    value: "/opt/strimzi"
  - name: "COM_REDHAT_COMPONENT"
    value: "amqstreams-bridge-container"

artifacts:
  - md5: 0c9082b3dcc560991f6f5ea42365b53c
    name: kafka-bridge.zip
  - md5: 4fff28d4ea6c97052f690a357eb0ed55
    name: kafka-bridge-licenses.tar.gz

packages:
  manager: microdnf
  content_sets:
    x86_64:
      - rhel-7-server-rpms
  install:
    - unzip
    - tar
    - rsync
    - shadow-utils # groupadd

execute:
  - script: install.sh
