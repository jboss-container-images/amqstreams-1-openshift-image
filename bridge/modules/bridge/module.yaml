schema_version: 1

name: bridge
version: 1.6.6

envs:
  - name: "STRIMZI_HOME"
    value: "/opt/strimzi"
  - name: "COM_REDHAT_COMPONENT"
    value: "amqstreams-bridge-container"

artifacts:
  - md5: bb014371edac82ea4c49c754c0e5cdb3
    name: kafka-bridge.zip
  - md5: 9c176a3e90351ebc5ef437f8ccd1c1d0
    name: kafka-bridge-licenses.tar.gz

packages:
  manager: microdnf
  content_sets:
    x86_64:
      - rhel-7-server-rpms
  install:
    - unzip
    - tar
    - rsync
    - shadow-utils # groupadd

execute:
  - script: install.sh
