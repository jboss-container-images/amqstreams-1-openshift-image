schema_version: 1

name: bridge
version: 1.6.5

envs:
  - name: "STRIMZI_HOME"
    value: "/opt/strimzi"
  - name: "COM_REDHAT_COMPONENT"
    value: "amqstreams-bridge-container"

artifacts:
  - md5: 0e5751f98a9329b873e6f21023b3777d
    name: kafka-bridge.zip
  - md5: 87c6841c7e28b66b1b01c9ee1963c0cc
    name: kafka-bridge-licenses.tar.gz

packages:
  manager: microdnf
  content_sets:
    x86_64:
      - rhel-7-server-rpms
  install:
    - unzip
    - tar
    - rsync
    - shadow-utils # groupadd

execute:
  - script: install.sh
