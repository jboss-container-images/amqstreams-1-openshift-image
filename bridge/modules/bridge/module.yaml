schema_version: 1

name: bridge
version: 2.7.0

envs:
  - name: "STRIMZI_HOME"
    value: "/opt/strimzi"
  - name: "COM_REDHAT_COMPONENT"
    value: "amqstreams-bridge-container"

artifacts:
  - md5: 9401e5adc30119bc971ee992ccfdbd86
    name: kafka-bridge.zip
  - md5: 1b8b9ae1ff1f03bd5285106a5d747692
    name: kafka-bridge-licenses.tar.gz

packages:
  manager: dnf
  install:
    - unzip
    - tar
    - rsync
    - shadow-utils # groupadd
    - gzip

execute:
  - script: install.sh
