schema_version: 1

name: amq7/amq-streams-entity-operator-stunnel
description: "AMQ Streams image for Entity Operator Stunnel sidecar"
version: "1.1.0"
from: amq7/amq-streams-stunnel-base:1.1.0

labels:
  - name: "com.redhat.component"
    value: "amqstreams11-entityoperatorstunnel-openshift-container"
  - name: "io.k8s.description"
    value: "Image for Entity Operator Stunnel sidecar"
  - name: "io.k8s.display-name"
    value: "AMQ Streams Entity Operator Stunnel sidecar"
  - name: "io.openshift.tags"
    value: "messaging,amq,jboss"

modules:
  repositories:
    - path: modules
  install:
    - name: entity-operator-stunnel

packages:
  content_sets:
    x86_64:
      - rhel-7-server-rpms

run:
  workdir: "/opt/stunnel"
  user: 185
  cmd:
    - "/opt/stunnel/stunnel_run.sh"

osbs:
  configuration:
    container:
      compose:
        pulp_repos: true
  repository:
    name: containers/amqstreams-1
    branch: rh-amqstreams-1.1-entityoperatorstunnel-openshift-rhel-7
