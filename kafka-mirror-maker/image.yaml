schema_version: 1

name: amq7/amq-streams-kafka-mirror-maker
description: "AMQ Streams image for Kafka"
version: "1.1.1"
from: amq7/amq-streams-kafka-base:1.1.1

labels:
  - name: "com.redhat.component"
    value: "amqstreams11-kafkamirrormaker-openshift-container"
  - name: "io.k8s.description"
    value: "A reliable and fault tolerant stream processing platform"
  - name: "io.k8s.display-name"
    value: "AMQ Streams Apache Kafka"
  - name: "io.openshift.tags"
    value: "messaging,amq,jboss"

modules:
  repositories:
    - path: modules
  install:
    - name: kafka-mirror-maker

packages:
  content_sets:
    x86_64:
      - rhel-7-server-rpms

run:
  user: 185
  cmd:
    - "/opt/kafka/kafka_mirror_maker_run.sh"

osbs:
  configuration:
    container:
      compose:
        pulp_repos: true
  repository:
    name: containers/amqstreams-1
    branch: rh-amqstreams-1.1-kafkamirrormaker-openshift-cp-rhel-7
